<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlut.forumx.user.mapper.UserMapper">

    <!-- 实体映射 -->
    <resultMap id="BaseResultMap" type="com.dlut.forumx.user.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="keycloak_id" property="keycloakId" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="bio" property="bio" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="TINYINT"/>
        <result column="birthday" property="birthday" jdbcType="DATE"/>
        <result column="location" property="location" jdbcType="VARCHAR"/>
        <result column="website" property="website" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP"/>
        <result column="last_login_ip" property="lastLoginIp" jdbcType="VARCHAR"/>
        <result column="register_time" property="registerTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, keycloak_id, username, email, phone, nickname, avatar, bio, gender, birthday,
        location, website, status, last_login_time, last_login_ip, register_time, update_time
    </sql>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.dlut.forumx.user.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            keycloak_id, username, email, phone, nickname, avatar, bio, gender,
            birthday, location, website, status, last_login_time, last_login_ip,
            register_time
        ) VALUES (
            #{keycloakId}, #{username}, #{email}, #{phone}, #{nickname}, #{avatar}, #{bio}, #{gender},
            #{birthday}, #{location}, #{website}, #{status}, #{lastLoginTime}, #{lastLoginIp},
            #{registerTime}
        )
    </insert>

    <!-- 根据ID更新用户 -->
    <update id="updateById" parameterType="com.dlut.forumx.user.entity.User">
        UPDATE user
        <set>
            <if test="keycloakId != null">keycloak_id = #{keycloakId},</if>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="location != null">location = #{location},</if>
            <if test="website != null">website = #{website},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据KeycloakID更新用户 -->
    <update id="updateByKeycloakId" parameterType="com.dlut.forumx.user.entity.User">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="location != null">location = #{location},</if>
            <if test="website != null">website = #{website},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
        </set>
        WHERE keycloak_id = #{keycloakId}
    </update>

    <!-- 更新最后登录信息 -->
    <update id="updateLastLogin">
        UPDATE user
        SET last_login_time = NOW(),
            last_login_ip = #{ip}
        WHERE id = #{userId}
    </update>

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 根据KeycloakID查询用户 -->
    <select id="selectByKeycloakId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE keycloak_id = #{keycloakId}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 批量查询用户 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 根据KeycloakID删除用户 -->
    <delete id="deleteByKeycloakId">
        DELETE FROM user
        WHERE keycloak_id = #{keycloakId}
    </delete>

</mapper>
